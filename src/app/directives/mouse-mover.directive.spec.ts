import { MouseMoverDirective } from './mouse-mover.directive';

describe('MouseMoverDirective', () => {
  it('should create an instance', () => {
    const directive = new MouseMoverDirective();
    expect(directive).toBeTruthy();
  });
});
